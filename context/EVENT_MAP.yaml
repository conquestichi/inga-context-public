kpi_guardrail_breach:
  severity: critical
  owner: trading_ops
  routes:
    - type: slack
      channel: '#trading-ops-critical'
      template: 'INGA: [CRIT] {{kpi}} breach: value={{value}} (warn={{warn}}/crit={{crit}}) window={{window}} at {{ts}}'
